---
name: zrh-fkct
description: Portable law-exam wrong-question review system with structured diagnosis, user-confirmed knowledge mapping, user-supplied XMind mind-map marking, Obsidian vault initialization, configurable weekly review automation, daily review, seven-day retesting, and learning-document generation. The bundled mind-map example currently covers 民诉; more subjects will be added later.
metadata:
  author: AI法师张诚
  portability: user-configured paths
  path_config: references/path-configuration.md
  version: 3.6
---

# 法考错题复盘系统

这是一个可移植的法考错题复盘 skill，不绑定某个用户、桌面目录、Obsidian vault、题库或科目图谱。

## 核心闭环

```text
原始资料 → 结构化识别 → 解析提点/明确追问 → 用户确认 → 入库 → 图谱标注 → 七日复测
```

## 功能

- **错题诊断**：识别题干、选项、答案、解析、表层知识点、根因和易混点。
- **确认门**：任何错题入库前，必须由用户显式确认知识点和一个错因标签；不得把 AI 候选或一句泛泛的“确认”当成用户选定的知识点。
- **复盘索引**：按日期、科目、单元和错因组织错题，`mistake-index.json` 是统计真源。
- **图谱标注**：入库时用**已确认知识点**标红/黄 `03-maps/<科目>/01-base/` 下的 `.xmind` 图谱，红色表示直接薄弱点（知识点不会），黄色表示易混点，输出 `-标注版.xmind` 到 `02-marked/`。思维导图输入必须是 `.xmind` 格式。
- **科目覆盖**：当前仓库只内置民诉示例，其他法考科目将在后续版本持续更新；用户也可以先上传对应科目的 `.xmind` 图谱。
- **Obsidian 初始化**：首次使用时创建核心复盘目录，科目目录和图谱子目录按实际使用情况创建，不预填空科目。
- **周复盘自动化**：支持入库后自动更新、macOS `launchd` 定时更新或手动生成三种模式。
- **七日复测**：记录 `passed`、`failed`、`skipped`，形成恢复闭环。
- **学习文档**：围绕薄弱点生成前置知识、主体讲解、易混点、思考题和反馈区。

## 输入

支持上传图片、粘贴 OCR/题目文本，或直接提供结构化 JSON。没有可信题库时，不声称题目精确匹配；没有足够证据时，标记低置信度或 `manual_required`。

## 输入处理规则（错题与知识分离）

用户可能只上传错误知识点、也可能上传错题+知识点、也可能只有错题。按以下规则处理：

| 输入 | 处理 |
| --- | --- |
| 仅错题且原始资料含解析 | 从解析提取候选知识点，向用户逐项确认后入库 |
| 仅错题且原始资料无解析 | 明确询问用户“这道题你不会/错在的知识点是什么”，收到具体知识点后再入库 |
| 错题+知识点 | 保留用户提供的知识点，要求用户确认知识点和错因后入库 |
| 仅错误知识点 | 整理前必须向用户确认，确认后才写入知识库 |

**原始资料优先提点**：先检查图片、OCR、题干附件和用户粘贴文本中是否含有“解析”“分析与思路”“错误分析”等内容。存在解析时，只能从解析、题干和选项的对应关系中提取候选知识点；必须清楚展示“从解析识别出的候选知识点”，并要求用户逐项选择、修改或补充。不存在解析时，必须直接问：`这道题你不会/错在的知识点是什么？请直接写知识点名称；不确定可写“不确定”。` 不得在无解析时替用户猜定知识点。

**图片输入回复顺序（强制）**：收到题目图片后、进行任何入库前，按以下顺序回复，不能只给结论或直接询问错因：

1. **识别出的原题**：原样转写题干、题号、选项、作答痕迹和已见答案；OCR 不清处标注“[识别不清]”，不得自行补写。
2. **识别出的完整原始解析**：原样转写图片中可见的全部“解析”“分析与思路”“命题思路与常见错误分析”等内容，不压缩、不改写、不只摘录结论；随后单列“从解析识别出的候选知识点”。确认入库后，错题卡必须保留该完整原文，并另存解析摘要。图片没有解析时，明确写“未提供解析”。
3. **知识点确认提问**：清楚要求用户写出最终要入库的知识点并选择错因 `1 知识点不会` 或 `2 知识点会但是做题思路不对`；说明候选仅供选择或修改，单独回复“确认”无效。

此阶段只能识别、展示和提问；不得生成错题卡、索引、复测队列、知识点文件、周复盘、图谱标注或产物汇总表。

**错题卡内容（强制精简）**：确认入库后的错题卡只保留“基本信息”“题干”“选项”“原始素材”“原始完整解析”五部分。解析区默认写入图片或原始材料中可见的完整原文，不再生成“解析摘要”“选入错题知识页的解析”“知识点”“证据”“下一步”等重复区块。用户确认的知识点只写入 `04-mistakes/by-knowledge/<科目>/`、科目汇集页和索引。

**确认后入库（强约束）**：用户必须同时给出具体知识点和一个错因标签（`1 知识点不会` / `2 知识点会但是做题思路不对`）。只有收到这两项明确输入，才生成错题卡、更新索引和复测队列、写入知识页；随后才生成 XMind 标注版。候选知识点、错因推断和“确认”这类不带知识点指向的回复都不能触发入库。

```yaml
---
id: "M-20260815-001"
type: "mistake"
subject: "民诉"
unit: ""
date: "2026-08-15"
primary_cause: "知识点不会"
counted: true
retest_due: "2026-08-22"
retest_status: "pending"
---
```

**错误知识点确认门（强约束）**：任何要写入知识库的知识点都必须经用户确认——包括用户单独列出的知识点（如「总是分不清 X 和 Y」）和错题附带的知识点。错题卡直接整理入库不受影响，但错题卡里的 `selected_knowledge_points` 未确认时**不进知识库**（不写 `by-knowledge` 文件、不进科目汇集页、不标图谱）。确认方式：用户确认知识点 + 一个错因标签（`1 知识点不会` / `2 知识点会但是做题思路不对`）。未确认不写入，导入输出会列出 `knowledge_pending_confirmation` 待确认卡。

**按科目归类**：错误知识点按科目归档到 `04-mistakes/by-knowledge/<科目>/`，每科目一个文件（如民诉的都在 `by-knowledge/民诉/`），知识点文件带 YAML frontmatter。`04-mistakes/by-subject/<科目>/错误知识点汇集.md` 汇总该科目全部错误知识点。

纯知识点条目（无题干）不生成错题卡、不进入图谱标注和每日「今日错题」统计。

## 路径规则

- 所有用户路径在运行时确定，禁止写死主目录、桌面、应用容器、Obsidian vault 或备份位置。
- 使用 `REVIEW_VAULT` 保存持久化复盘文件。
- 使用 `TECHNICAL_WORKSPACE` 保存临时转换、图谱报告和调试产物。
- 使用 `SOURCE_MATERIALS` 表示用户提供的错题图片、OCR、PDF 或题库文件；思维导图输入单独使用 `SOURCE_XMIND`，且必须是 `.xmind` 格式。
- 使用 `SOURCE_XMIND` 指定用户上传的 XMind 思维导图；不把 OPML、PDF 或图片当作本功能的思维导图输入。
- 使用 `GRAPH_FILE` 指定当前科目的图谱；内置图谱只作为相关科目的 fallback。
- 使用 `TARGET_VAULT` 和 `BACKUP_DIR` 处理用户明确选择的 Obsidian 写入和备份。
- 使用 `REVIEW_VAULT/00-index/skill-config.json` 保存用户确认过的科目创建模式和周复盘触发方式。
- 路径解析和脚本示例见 `references/path-configuration.md`。

## 安装后配置引导（首次调用必做）

客户安装本 skill 后，首次调用时必须先引导完成配置，**不得直接进入错题录入或图谱标注**。

按以下顺序逐项引导，每项用一句话说明用途，等用户确认后再写入，不替用户做主：

1. **Vault**：询问用户选择已有 Obsidian Vault 或新建 Vault，确认 `REVIEW_VAULT`。
2. **工作区**：与用户确认 `TECHNICAL_WORKSPACE`（存放临时转换、图谱报告和调试产物）。
3. **科目图谱**：确认当前科目的图谱来源。内置民诉图谱仅作 fallback；其他科目需用户上传 `.xmind` 格式图谱（`SOURCE_XMIND`），不接受 OPML、PDF 或图片。
4. **周复盘方式**：macOS 用户询问 `on_ingest`（推荐）/ `launchd` / `manual`；非 macOS 默认 `on_ingest`，不展示 macOS 专属选项。
5. **持久化**：将选择写入 `00-index/skill-config.json`；目录不存在则初始化核心目录，只创建缺失文件，不覆盖已有内容。

引导规则：

- 配置只询问一次，写入后不重复询问，除非用户要求更换。
- 用户未选择持久化位置时，保持预览模式。
- 配置完成后，向用户展示配置摘要与下一步操作（如「已就绪，可录入第一道错题」）。
- `launchd` 模式只在用户明确选择后才生成或安装 LaunchAgent 配置。

周复盘方式说明：

- `on_ingest`：确认入库或完成复测后自动更新本周和上周周复盘，推荐。
- `launchd`：使用 macOS LaunchAgent 每周定时生成。
- `manual`：不自动触发，只在用户要求时生成。

## 运行规则

1. 先从原始资料的解析中提取候选知识点；无解析时明确追问用户知识点，不入库。
2. 展示解析和候选知识点（如有），要求用户显式写出最终知识点，并选择 `1` 或 `2`；仅回复“确认”无效。
3. 只在确认后写入错题卡、索引、复测队列和知识页，生成稳定 `mistake_id` 和七日复测日期。
4. 入库完成后才对 `03-maps/<科目>/01-base/` 的 XMind 输出标注版。
5. 错误知识点按科目归档到 `04-mistakes/by-knowledge/<科目>/`。
6. `on_ingest` 模式下，入库后自动生成或更新当前周和上一周的 `02-weekly/YYYY-Www.md`。
7. `launchd` 模式只由用户选择后启用；`manual` 模式只在用户明确要求时运行周复盘脚本。
8. 图谱标注：入库时对 `03-maps/<科目>/01-base/` 下的 `.xmind` 图谱，用**已确认知识点**标红（知识点不会）/标黄（易混点），输出到 `02-marked/`。未确认知识点不标图谱；没有 `.xmind` 图谱的科目跳过标注。
9. 基础图谱不预先染色；标注输出与源图谱分离。
10. 所有推荐知识点都必须能追溯到题目、解析、用户标签或图谱关系。

## 入库完成产物汇总（强制）

仅在用户已明确确认知识点和错因、错题已成功入库、并且 XMind 图谱标注已执行完成（成功或因无图谱而明确跳过）后，才能在最终回复中输出“任务产物汇总”表。确认、预览、OCR 识别、配置等未入库阶段禁止输出该表。

| 产物 | 位置 | 状态 |
| --- | --- | --- |
| 错题卡（带 YAML frontmatter） | `01-daily/<日期>/2-整理好的错题/<文件>.md` | `✅` 已写入；或 `⏸` 待知识点确认 |
| 原始材料 | `01-daily/<日期>/1-原始错题素材/` | 写明复制数量、文件类型或未提供 |
| 统计索引 | `00-index/mistake-index.json` | 写明新增/更新的 `mistake_id`，或未写入原因 |
| 复测队列 | `00-index/retest-queue.json` | 写明 `retest_due` 和状态，或未写入原因 |
| 每日复盘 | `01-daily/<日期>/<日期>.md` | 写明是否更新 |
| 科目汇集页 | `04-mistakes/by-subject/<科目>/错误知识点汇集.md` | 写明新增知识点数量，或未写入原因 |
| 知识点文件 | `04-mistakes/by-knowledge/<科目>/<文件>.md` | 写明是否按科目归类 |
| 周复盘 | `02-weekly/YYYY-Www.md` | 写明触发方式与实际更新的周文件；`manual` 未执行时标明未生成 |
| 图谱标注 | `03-maps/<科目>/02-marked/<文件>-标注版.xmind` | 写明标注颜色/节点数，或没有 `.xmind` 时标明跳过 |
| 导入/执行报告 | 实际报告路径 | 写明是否生成；没有报告文件时标明未生成 |

规则：

- 只列出本次工作流适用的产物；不要凭空声称文件已生成。
- 路径使用相对 `REVIEW_VAULT` 的路径；可多行但必须保持表格可读。
- 状态列必须以 `✅`、`⏸`、`⚠️` 或 `❌` 开头，并给出结果、数量、日期或未执行原因。
- 产物表排在所有写入、周复盘更新和图谱标记完成之后；图谱标记行必须说明实际输出的 `02-marked/` 文件及颜色/节点数，或说明跳过原因。
- 图谱标记成功后，在产物表的最后主动输出每个标注版 `.xmind` 的 Markdown 本地链接，使用绝对路径，例如：`思维导图：[打开标红版民诉图谱](/绝对路径/民诉法-标注版.xmind)`；不得要求用户自行到文件夹查找。

## 脚本

- `scripts/init-review-vault.js`：初始化复盘库目录。
- `scripts/configure-automation.js`：保存周复盘触发方式，并按用户选择生成可选的 macOS LaunchAgent 配置。
- `scripts/generate-weekly-review.js`：从错题索引和复测队列生成当前周与上一周周复盘。
- `scripts/prepare-confirmation.js`：生成错题入库确认单。
- `scripts/prepare-knowledge-confirmation.js`：生成错误知识点入库确认单（确认后才写入知识库）。
- `scripts/ingest-mistakes.js`：写入错题卡、索引、复测队列和科目视图。
- `scripts/metrics-dashboard.js`：生成指标面板。
- `scripts/generate-learning-doc.js`：生成学习文档骨架。
- `scripts/update-retest.js`：记录七日复测结果。
- `scripts/mark-graph.js`：标记 JSON 知识图谱。
- `scripts/mark-xmind.js`：用已确认知识点标红/黄 `.xmind` 图谱，输出 `-标注版.xmind`；入库时自动联动标注 `03-maps/<科目>/01-base/` 下的图谱。
- `scripts/render-graph-view.js`：生成浏览器图谱报告。

## 复盘库结构

```text
00-index/                         # 统计真源和复测队列
01-daily/YYYY-MM-DD/              # 每日原始材料与已确认错题
02-weekly/                        # 周复盘
03-maps/<科目>/                    # 按需创建的科目图谱
  00-source/                       # 原始 .xmind
  01-base/                         # 基础图谱
  02-marked/                       # 标注图谱
04-mistakes/by-subject/<科目>/     # 科目薄弱点
04-mistakes/by-cause/<错因>/       # 错因视图
04-mistakes/by-knowledge/<科目>/   # 按科目归档的错误知识点
05-learning-docs/                 # 学习文档
```

## 质量边界

- 保留题干、选项和解析原文，不擅自改写用户材料。
- 不在没有可信题库时伪造精确题目来源。
- 不把低置信度法律解释当成确定事实。
- 用户没有选择持久化位置时，保持预览模式。
- `大衣` 作为历史输入兼容词时归一化为 `大意`。
